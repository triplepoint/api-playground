[project]
name = "api-playground"
version = "0.1.0"
description = "A simple demonstration API with placeholder data."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.121.2",
    "markdown>=3.10",
    "requests>=2.32.5",
]
